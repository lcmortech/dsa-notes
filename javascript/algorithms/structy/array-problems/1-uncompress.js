const compress = (str) => {
    nums = '0123456789' //for reference
    const result = ''
    let i = 0
    let j = 0 //iterates over the nums, ends at first char

    while (j < str.length){
        if(nums.includes(s[j])){

        } else { //fires at the end of the number
            const num = Number(s.slice(i, j)) //(starts at i, ends bf j)
        }
    }

    return result
}