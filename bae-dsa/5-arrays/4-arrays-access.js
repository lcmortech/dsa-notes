/*
ACCESS
Accessing an array at a specified index only takes O(1) because this process uses that indes to get the value directly from the address in memory. It is done by specifying the index (remember that indexing starts at 0).
let array1 = [1,2,3,4]
array[1] //returns 2
*/